# Vue logout action

## Prompt

Continue the active migration goal:

1. Bring the environment up with Docker.
2. Complete migration against `../FoolFrame`.
3. Use Vue for the frontend.
4. Commit atomically and promptly.

## Scope

- Added a typed Vue payload builder for common token-only requests.
- Added a Logout action to the existing Vue Token & Profile panel.
- Cleared the stored frontend token after successful logout.
- Updated the migration parity document.

## Changed Files

- `frontend/src/App.vue`
- `frontend/src/payload.ts`
- `frontend/src/payload.test.ts`
- `docs/migration/foolframe-parity.md`

## Validation

- RED: `cd frontend && npm test`
  - Failed before implementation with `buildTokenRequest is not a function`.
- GREEN: `cd frontend && npm test`
  - Passed: 1 file, 11 tests.
- `cd frontend && npm run build`
  - Passed `vue-tsc --noEmit` and Vite production build.
- `DOCKER_BUILDKIT=0 docker build -t fool-service-frontend ./frontend`
  - Passed using already-present local `node:20-alpine` and `nginx:1.27-alpine`
    base images.
- `docker compose up -d --no-build --force-recreate frontend`
  - Recreated the frontend container from the new local image.
- `curl http://localhost:8081/`
  - Returned `/assets/index-Dyi0fJVj.js`.
- `curl http://localhost:8081/assets/index-Dyi0fJVj.js | rg -o "Logout|logout|fool-service-token"`
  - Found the new Vue logout UI strings in the running asset.
- Login/profile/logout/profile-after-logout HTTP smoke:
  - `POST /api/v1/auth/login` with `admin` / `admin` returned a token.
  - `POST /api/v1/auth/profile` with that token returned the seeded admin.
  - `POST /api/v1/auth/logout` returned `code:0`.
  - `POST /api/v1/auth/profile` with the logged-out token returned
    `code:8100004` / `token无效`.
- `docker compose ps`
  - Backend, frontend, MySQL, and Redis are running; MySQL and Redis are
    healthy.
- `python scripts/check_repo_harness.py`
  - Passed.

## Skipped Checks

- No browser screenshot was captured; the slice was verified through the built
  and running frontend asset plus backend auth token-invalidation smoke.

## Risks

- Logout clears only this console's stored token. It does not clear any other
  browser session state beyond `fool-service-token`.
