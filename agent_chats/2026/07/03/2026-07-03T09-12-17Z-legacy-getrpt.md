# Legacy getrpt

## Prompt

Continue the active goal: keep Docker running, migrate against `../FoolFrame`,
use Vue for the frontend, and commit in atomic slices.

## Scope

- Compared `IDataService.GetReport`, `DataService.GetReport`, and
  `HandlerMakeReport` in `../FoolFrame`.
- Exposed `POST /api/v1/report/getrpt` as the legacy route for the already
  migrated flat-grid report path.
- Added a Vue operator console button that calls `getrpt` with the same
  `MakeReportOption` payload as `makereport`.
- Updated the migration parity document.

## Changed Files

- `fool-view/src/main/java/org/fool/framework/view/api/ReportController.java`
- `fool-view/src/test/java/org/fool/framework/view/api/ReportControllerTest.java`
- `frontend/src/App.vue`
- `frontend/src/payload.test.ts`
- `docs/migration/foolframe-parity.md`
- `agent_chats/2026/07/03/2026-07-03T09-12-17Z-legacy-getrpt.md`

## Validation

- RED backend:
  `docker run --rm --network fool-service_default -v "$PWD":/workspace -v "$PWD/.m2-docker":/root/.m2 -w /workspace maven:3.9-eclipse-temurin-17 mvn -pl fool-view -am -DfailIfNoTests=false -Dtest=ReportControllerTest test`
  failed before implementation because `makeReport` did not expose
  `/getrpt`.
- RED frontend:
  `npm test -- --run payload.test.ts` failed before implementation because the
  Vue console did not expose `/api/v1/report/getrpt`.
- GREEN backend:
  `docker run --rm --network fool-service_default -v "$PWD":/workspace -v "$PWD/.m2-docker":/root/.m2 -w /workspace maven:3.9-eclipse-temurin-17 mvn -pl fool-view -am -DfailIfNoTests=false -Dtest=ReportControllerTest test`
  passed: 10 tests, 0 failures, 0 errors.
- GREEN frontend:
  `npm test -- --run payload.test.ts` passed: 31 tests.
- GREEN frontend build:
  `npm run build` passed.
- Docker rebuild:
  `docker compose up -d --build` rebuilt backend/frontend and restarted the
  Compose stack.
- Docker runtime smoke:
  `POST /api/v1/report/getrpt` passed through both `http://localhost:8080`
  and `http://localhost:8081`, returning report cells with `Symbol` and
  `State` headers for the Docker `OrderList` smoke data.

## Skipped Checks

- Did not add a separate report engine for `getrpt`; FoolFrame routes
  `getrpt` through `HandlerMakeReport`, so the migrated route reuses the
  existing `makereport` implementation.
- Broader saved-report execution/export work remains open migration work.
