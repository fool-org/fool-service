# 2026-07-03T04:37:35Z DB Manage Seed

## Prompt

- Continue the active goal: Docker runtime, FoolFrame migration parity, Vue
  frontend, and timely atomic commits.

## Scope

- Seed Docker smoke rows for legacy DB-management tables.
- Patch the running Docker MySQL volume to match the edited init SQL.

## Changed Files

- `docker/mysql/init/003-db-manage.sql`
- `docs/migration/foolframe-parity.md`
- `agent_chats/2026/07/03/2026-07-03T04-37-35Z-db-manage-seed.md`

## Validation

- RED: `docker compose exec -T mysql mysql -uroot -pPa88word car_wash -e "SELECT 'DB_App' AS table_name, COUNT(*) AS c FROM DB_App UNION ALL SELECT 'WorkDataBase', COUNT(*) FROM WorkDataBase UNION ALL SELECT 'DB_AppDB', COUNT(*) FROM DB_AppDB UNION ALL SELECT 'DS_DataSourceSet', COUNT(*) FROM DS_DataSourceSet;"`
  - `DB_App`, `WorkDataBase`, `DB_AppDB`, and `DS_DataSourceSet` all returned
    count `0`.
- GREEN: `docker compose exec -T mysql mysql -uroot -pPa88word car_wash -e "SELECT BO_Id,BO_AppName FROM DB_App ORDER BY BO_Id; SELECT DBNo,DBName,DBYear,DBSysName,IsActive,HEX(pwd1) AS pwd1,HEX(pwd2) AS pwd2,HEX(pwd3) AS pwd3,HEX(pwd4) AS pwd4,pwd5,UserName,CompanyName,ServerIp,IsLocal FROM WorkDataBase ORDER BY DBNo; SELECT App_Id,DBNo FROM DB_AppDB ORDER BY App_Id,DBNo; SELECT DS_Key,DS_DBNo FROM DS_DataSourceSet ORDER BY DS_Key;"`
  - Returned app `fool-service`, working database `01 / car_wash / 2026`,
    app-db relation `1 / 01`, and datasource key `car_wash / 01`.
- GREEN: Docker JDK `jshell` with `LegacyPasswordCipher` and
  `LegacySqlLiterals` decrypted the seeded `pwd1..pwd5` payload to `Pa88word`.
- `curl -fsS http://localhost:8080/test`
  - Returned the Docker smoke order rows.
- `python scripts/check_repo_harness.py`
  - Passed.

## Skipped

- Did not add a DB-management REST or UI surface; this slice only makes the
  already migrated legacy catalog tables usable in fresh Docker volumes.

## Risks

- Existing Docker volume was patched manually; fresh volumes get the same rows
  from `003-db-manage.sql`.
