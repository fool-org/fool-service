# Vue saveobj writeback panel

## Prompt

Continue the active migration goal:

1. Bring the environment up with Docker.
2. Complete migration against `../FoolFrame`.
3. Use Vue for the frontend.
4. Commit atomically and promptly.

## Scope

- Added a typed Vue payload builder for the legacy `/api/v1/data/saveobj`
  request shape.
- Added a visible Save Object panel to the existing Vue migration console.
- Supported simple `Propertyies` and `Itemproperties` JSON payloads.
- Updated the migration parity document.

## Changed Files

- `frontend/src/App.vue`
- `frontend/src/payload.ts`
- `frontend/src/payload.test.ts`
- `docs/migration/foolframe-parity.md`

## Validation

- RED: `cd frontend && npm test`
  - Failed before implementation with `buildSaveObjRequest is not a function`.
- GREEN: `cd frontend && npm test`
  - Passed: 1 file, 5 tests.
- `cd frontend && npm run build`
  - Passed `vue-tsc --noEmit` and Vite production build.
- `docker compose up -d --build frontend`
  - Built frontend and backend images.
- `docker compose up -d --force-recreate frontend`
  - Recreated frontend and backend containers so the running Nginx asset used
    the new build.
- `curl http://localhost:8081/`
  - Returned `/assets/index-C9JDdm5E.js`.
- `curl http://localhost:8081/assets/index-C9JDdm5E.js | rg -o "Save Object|saveobj|Propertyies JSON"`
  - Found the new Vue save-object UI strings in the running asset.
- `curl http://localhost:8080/test`
  - Returned the seeded order rows.
- `curl -H 'Content-Type: application/json' -d '{"saveObj":{"id":"1001","viewID":"100","propertyies":[{"key":"symbol","value":"BTC-USDT"},{"key":"state","value":"OPEN"}]}}' http://localhost:8080/api/v1/data/saveobj`
  - Returned `{"code":0,"message":"success","data":null}`.
- `docker compose exec -T mysql mysql -uroot -pPa88word car_wash -e 'SELECT order_id,order_symbol,order_state FROM market_order WHERE order_id = 1001;'`
  - Returned `1001`, `BTC-USDT`, `OPEN`.
- `docker compose ps`
  - Backend, frontend, MySQL, and Redis are running; MySQL and Redis are healthy.
- `python scripts/check_repo_harness.py`
  - Passed.
- `git diff --check`
  - Passed.

## Skipped Checks

- No browser screenshot was captured; this slice uses the existing console
  layout and was verified through the built/running asset and backend writeback.

## Risks

- The panel accepts raw JSON for item collections. A richer editor can replace
  it once the full detail-edit workflow is migrated.
