# Legacy getapp AppInfo

## Prompt

Continue the Docker/FoolFrame/Vue migration and keep atomic commits.

## Scope

- Added the legacy `/api/v1/auth/getapp` surface.
- Reused `fool-app-manage` `SW_APPLICATION` metadata for the default legacy
  `AppInfo`.
- Updated `getmain` to return the same default `AppInfo` instead of an empty
  shell.
- Added the Vue operator-console `App Info` action and response capture.

## Changed Files

- `fool-auth/pom.xml`
- `fool-auth/src/main/java/org/fool/framework/auth/api/LoginController.java`
- `fool-auth/src/main/java/org/fool/framework/auth/business/service/AuthService.java`
- `fool-auth/src/test/java/org/fool/framework/auth/api/LoginControllerLogoutTest.java`
- `fool-auth/src/test/java/org/fool/framework/auth/business/service/AuthServiceLegacyMenuTest.java`
- `frontend/src/App.vue`
- `frontend/src/api.ts`
- `frontend/src/payload.test.ts`
- `docs/migration/foolframe-parity.md`

## Validation

- `docker run --rm --network fool-service_default -v "$PWD":/workspace -v "$PWD/.m2-docker":/root/.m2 -w /workspace maven:3.9-eclipse-temurin-17 mvn -pl fool-auth -am -DfailIfNoTests=false -Dtest=LoginControllerLogoutTest,AuthServiceLegacyMenuTest test`
  - Passed: 11 tests.
- `npm test`
  - Passed: 29 tests.
- `npm run build`
  - Passed.
- `python scripts/check_repo_harness.py`
  - Passed.
- `git diff --check`
  - Passed.
- `docker compose up -d --build`
  - Passed; backend and frontend images rebuilt.
- Runtime smoke:
  - `GET http://localhost:8080/test` returned HTTP 200.
  - `GET http://localhost:8081/` returned HTTP 200.
  - `POST http://localhost:8080/api/v1/auth/login` with admin/admin returned a token.
  - `POST /api/v1/auth/getapp` through ports 8080 and 8081 returned
    `AppName=Fool Service`, `AppId=fool-service`, and `DefaultViewId=100`.
  - `POST /api/v1/auth/getmain` through ports 8080 and 8081 returned the same
    `AppInfo`.

## Skipped / Risk

- Full FoolFrame `loginv2` AppId/AppKey/DbId session storage is still future
  migration work. This slice uses the seeded default app until tokens carry app
  context.
