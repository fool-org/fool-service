# Menu Images

## Prompt

- Continue aligning Vue layout, style, and interaction behavior with old
  FoolFrame, with every behavior change committed atomically.

## Scope

- Compare `tbar.jade`, dynamic child-menu rendering, and `.sw-menuimg` with
  `LegacyMenuNav.vue`.
- Adapt Pascal/camel `ImageUrl` once in `viewWorkflow`.
- Render top and child menu images at 30x30 only when metadata supplies a URL.
- Preserve labels, notification counts, menu selection, and empty-image layout.

## Changed Files

- `frontend/src/viewWorkflow.ts`
- `frontend/src/viewWorkflow.test.ts`
- `frontend/src/LegacyMenuNav.vue`
- `frontend/src/style.css`
- `frontend/src/payload.test.ts`
- `tasks.md`
- `docs/migration/foolframe-parity.md`
- `agent_chats/2026/07/12/2026-07-12T11-44-58Z-menu-images.md`

## Validation

- `cd frontend && npm test -- --run`: 9 files, 147 tests passed.
- `cd frontend && npm run build`: TypeScript and Vite production build passed.
- `docker compose build frontend`: passed.
- `python scripts/runtime_doctor.py`: all checks passed, including menu loading,
  notifications, login/logout, and View-first workflows.
- `python scripts/check_repo_harness.py`: passed.
- `git diff --check`: passed.

## Runtime Evidence

- Rebuilt frontend image deployed at `http://localhost:8081`.
- Docker menu rows currently have null `AUTH_MENU_IMAGE`, confirming the
  no-placeholder runtime path.
- `docker compose ps -a`: backend/frontend up, MySQL/Redis healthy, and
  `db-migrate` exited successfully with code 0.

## Skipped Or Downgraded Checks

- The Docker seed has no non-empty menu image; that branch is covered by the
  adapter and component source assertions.
- Authenticated visual inspection requires a fresh CAPTCHA authorization.

## Risks And Follow-Ups

- Final browser acceptance should verify a migrated non-empty menu image URL
  on both desktop navigation and the mobile Drawer.
