# Auth logout

## Prompt

- Continue the active migration goal:
  1. run the environment with Docker;
  2. complete migration against `../FoolFrame`;
  3. use Vue for the frontend;
  4. make timely atomic commits.

## Scope

- Added `POST /api/v1/auth/logout` as the Vue replacement for legacy
  `IDataService.logout`.
- Added token invalidation in `TokenService` for both token-to-user and
  user-to-token Redis keys.
- Added Vue `CommonRequest` type for token-only requests.
- Updated migration parity notes with logout smoke coverage.

## Changed Files

- `fool-auth/src/main/java/org/fool/framework/auth/api/LoginController.java`
- `fool-auth/src/main/java/org/fool/framework/auth/business/service/AuthService.java`
- `fool-auth/src/main/java/org/fool/framework/auth/business/service/TokenService.java`
- `fool-auth/src/test/java/org/fool/framework/auth/api/LoginControllerLogoutTest.java`
- `fool-auth/src/test/java/org/fool/framework/auth/business/service/TokenServiceLogoutTest.java`
- `frontend/src/api.ts`
- `docs/migration/foolframe-parity.md`
- `agent_chats/2026/07/02/2026-07-02T21-13-39Z-auth-logout.md`

## Validation

- RED:
  `docker run --rm -v "$PWD":/workspace -v "$HOME/.m2":/root/.m2 -w /workspace maven:3.9-eclipse-temurin-17 mvn -DfailIfNoTests=false -pl fool-auth -am -Dtest=TokenServiceLogoutTest,LoginControllerLogoutTest test`
  failed because `logoutToken`, `AuthService.logout`, and
  `LoginController.logout` were missing.
- GREEN:
  same focused command passed with 2 tests.
- Backend:
  `docker run --rm --network fool-service_default -v "$PWD":/workspace -v "$HOME/.m2":/root/.m2 -w /workspace maven:3.9-eclipse-temurin-17 mvn -DfailIfNoTests=false -pl fool-auth -am test`
  passed.
- Frontend:
  `cd frontend && npm test && npm run build` passed.
- Harness:
  `python scripts/check_repo_harness.py` passed.
- Whitespace:
  `git diff --check` passed.
- Docker:
  `docker compose up -d --build backend frontend` passed.

## Runtime Evidence

- `docker compose ps` showed backend, frontend, MySQL, and Redis running.
- `curl -H 'Content-Type: application/json' -d '{"userId":"admin","password":"admin"}' http://localhost:8080/api/v1/auth/login`
  returned `code:0` and a token.
- `curl -H 'Content-Type: application/json' -d '{"token":"<token>"}' http://localhost:8080/api/v1/auth/logout`
  returned `code:0`.
- Reusing the same token with `/api/v1/auth/profile` returned a nonzero code
  and `token无效`.

## Skipped Checks

- No browser screenshot check; this is an auth API compatibility slice.

## Risks / Follow-ups

- This maps logout to the existing `/api/v1/auth/*` API namespace instead of
  adding a root-level `/logout` WCF-compatible path.
