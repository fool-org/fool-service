# Auth Schema Parity

## Prompt

- Continue the active FoolFrame migration goal.
- Current slice: close the Docker schema gap for the Vue auth workflow.

## Scope

- Added Docker MySQL schema for `auth_user`, `auth_role`, `auth_item`,
  `auth_user_role`, and `auth_role_auth`.
- Updated `docs/migration/foolframe-parity.md`.

## Changed Files

- `docker/mysql/init/007-auth.sql`
- `docs/migration/foolframe-parity.md`
- `agent_chats/2026/07/02/2026-07-02T10-38-40Z-auth-schema.md`

## Validation

- Schema:
  `docker compose exec -T mysql mysql -uroot -pPa88word car_wash < docker/mysql/init/007-auth.sql`
  completed, and `SHOW TABLES LIKE 'auth_%'` showed `auth_item`,
  `auth_role`, `auth_role_auth`, `auth_user`, and `auth_user_role`.
- Runtime auth endpoint:
  `curl -s -i -H 'Content-Type: application/json' -d '{"userId":"admin","password":""}' http://localhost:8080/api/v1/auth/login`
  returned HTTP 200 with business error `用户不存在`, confirming the path now
  reaches `auth_user` instead of failing on a missing table.
- Repository harness:
  `python scripts/check_repo_harness.py` passed.
- Whitespace:
  `git diff --check` passed with no output.
- Runtime stack:
  `docker compose ps` showed backend and frontend up, with MySQL and Redis
  healthy.

## Risks

- This slice only adds the tables required by the existing Vue auth calls.
  It does not seed admin users or change the current password hashing behavior.
