# Query Paged SQL Builder Parity

## Scope

- Compared legacy `../FoolFrame/src/Server/SWDQ01-Soway.Query/SqlScriptFac.cs` `Result(...)`.
- Migrated the paged SQL wrapper around the base select SQL generated by `QuerySqlBuilder`.
- Preserved legacy count query plus page query shape while using Java/JDBC positional placeholders through `QueryAndArgs`.
- Kept report-parameter binding and concrete JDBC execution as later slices.

## Changes

- Added `QuerySqlBuilder.pagedSql(QueryInstance, int, int)`.
- Added `QuerySqlBuilder.pagedSql(QueryInstance, int, int, String, boolean)`.
- The paged SQL wrapper projects selected column aliases, optionally includes the row-index alias, and appends filter arguments before repeated page/page-size arguments.
- Updated `docs/migration/foolframe-parity.md` to mark paged SQL wrapping and filter/page parameter payload support as migrated.

## TDD

- Red:
  - `docker run --rm -v "$PWD":/workspace -w /workspace -v "$HOME/.m2":/root/.m2 maven:3.9-eclipse-temurin-17 mvn -pl fool-query -am -Dtest=QuerySqlBuilderTest -DfailIfNoTests=false test`
  - Result: test compile failed because `QuerySqlBuilder.pagedSql(...)` overloads did not exist.
- Green:
  - Same command.
  - Result: `QuerySqlBuilderTest`: 6 tests, 0 failures, 0 errors.

## Verification

- Focused query regression:
  - `docker run --rm -v "$PWD":/workspace -w /workspace -v "$HOME/.m2":/root/.m2 maven:3.9-eclipse-temurin-17 mvn -pl fool-query -am -Dtest=QuerySqlBuilderTest,QueryInstanceMigrationTest,QueryLookupCollectionTest,SelectTypeCatalogTest,SelectedColumnCollectionTest,SelectedTablesTest,CompareOpCatalogTest,CompareFilterTest -DfailIfNoTests=false test`
  - Result: `SelectedTablesTest`, `CompareFilterTest`, `QueryLookupCollectionTest`, `QuerySqlBuilderTest`, `QueryInstanceMigrationTest`, `SelectTypeCatalogTest`, `CompareOpCatalogTest`, and `SelectedColumnCollectionTest`: 26 tests, 0 failures, 0 errors.
- Full backend package on the Compose network:
  - Result: `BUILD SUCCESS` across all 15 backend reactor modules in 43.184 s.
- Backend compose image/runtime:
  - `docker compose build -q backend && docker compose up -d backend`: backend image rebuilt and container restarted.
  - `docker compose ps`: backend, frontend, mysql, and redis running; MySQL and Redis healthy.
- Runtime smoke after backend restart:
  - `GET http://localhost:8080/test`: response captured, 132 bytes.
  - `GET http://localhost:8081/`: response captured, 399 bytes.
  - `POST http://localhost:8080/api/v1/data/query-list` with `OrderList` `orderId` filter values `1001,1002`: response captured, 296 bytes.
- Repository hygiene:
  - `python scripts/check_repo_harness.py`: passed.
  - `git diff --check`: passed.

## Remaining

- `SqlScriptFac.Result` concrete JDBC execution remains a separate migration slice.
- Report-parameter binding remains tied to later bool-expression/report parameter integration.
